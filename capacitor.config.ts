import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-standalone-consume-api-movie',
  webDir: 'www'
};

export default config;
